/**
 * Page sizes for offset-paginated lists (server) and the load-more buttons
 * (client). Shared so both sides agree on when "more" remains.
 */
export const LIST_PAGE_SIZE = 25
export const ATTENDANCE_PAGE_SIZE = 200